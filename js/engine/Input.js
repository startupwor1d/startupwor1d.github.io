export class Input{constructor(){this.keys=new Set;this.justPressed=new Set;addEventListener("keydown",e=>{if(!this.keys.has(e.key))this.justPressed.add(e.key);this.keys.add(e.key);if(["ArrowUp","ArrowDown","ArrowLeft","ArrowRight","e","E"].includes(e.key))e.preventDefault()});addEventListener("keyup",e=>this.keys.delete(e.key));addEventListener("blur",()=>{this.keys.clear();this.justPressed.clear()})}update(){this.justPressed.clear()}down(k){return this.keys.has(k)}pressed(k){return this.justPressed.has(k)}}
